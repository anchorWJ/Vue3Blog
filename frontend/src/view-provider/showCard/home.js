import { ref } from "@vue/reactivity"

export const homeCardLeftViewConfig = {
  homeCardLeftRecords: ref([]),
    // limit: ref(10),
    // total: ref(0)
}

export const homeCardRightViewConfig = {
  homeCardRightRecords: ref([]),
  // limit: ref(10),
  // total: ref(0)
}